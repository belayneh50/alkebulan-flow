# Optional: enables future credential-backed generation. The current demo fallback needs no key.
AI_GATEWAY_API_KEY=

# Production integrations planned for the persistence stage.
DATABASE_URL=
AUTH_SECRET=
