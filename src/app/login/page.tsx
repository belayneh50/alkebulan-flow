"use client";
import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, Check, Sparkles, Zap } from "lucide-react";
import { Button, Card, Input } from "@/components/ui/primitives";

export default function LoginPage(){const [sent,setSent]=useState(false);const router=useRouter();return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-[#13251a] p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3 font-bold"><span className="grid size-9 place-items-center rounded-xl bg-emerald-500"><Zap className="size-5" fill="currentColor"/></span>Alkebulan Flow</div><div className="max-w-lg"><span className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/5 px-3 py-1.5 text-xs"><Sparkles className="size-3"/>AI Operations Hub</span><h1 className="mt-6 text-5xl font-bold leading-[1.05] tracking-tight">Run client work with clarity, not chaos.</h1><p className="mt-5 text-lg leading-8 text-emerald-50/70">Projects, tasks, files, and next actions—together in one calm workspace.</p><div className="mt-9 space-y-4 text-sm text-emerald-50/80">{["See every deadline before it becomes urgent","Turn project activity into client-ready updates","Give each teammate exactly the access they need"].map(x=><div key={x} className="flex items-center gap-3"><Check className="size-4 text-emerald-400"/>{x}</div>)}</div></div><p className="text-xs text-emerald-50/40">A portfolio demonstration. No public contact details are collected.</p></section><section className="grid place-items-center p-5"><Card className="w-full max-w-md p-7 md:p-9"><div className="mb-8 lg:hidden"><div className="flex items-center gap-3 font-bold"><span className="grid size-9 place-items-center rounded-xl bg-emerald-700 text-white"><Zap className="size-5"/></span>Alkebulan Flow</div></div><p className="text-xs font-bold uppercase tracking-[.16em] text-emerald-700">Welcome back</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Sign in to your workspace</h2><p className="mt-2 text-sm text-muted-foreground">Use the demo access below to explore every feature.</p><form className="mt-7 space-y-4" onSubmit={e=>{e.preventDefault();router.push("/")}}><div><label className="mb-1.5 block text-xs font-bold">Workspace email</label><Input type="email" defaultValue="demo@alkebulan.local" required/></div><div><div className="mb-1.5 flex justify-between"><label className="text-xs font-bold">Password</label><button type="button" onClick={()=>setSent(true)} className="text-xs font-bold text-emerald-700">Forgot password?</button></div><Input type="password" defaultValue="FlowDemo2026!" required/></div>{sent&&<p className="rounded-xl bg-emerald-50 p-3 text-xs text-emerald-800">Demo reset instructions prepared. Production delivery requires an email provider.</p>}<Button className="w-full">Enter demo workspace <ArrowRight className="size-4"/></Button></form><div className="my-6 flex items-center gap-3 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border"/>Demo access<span className="h-px flex-1 bg-border"/></div><Link href="/" className="block rounded-xl border px-4 py-3 text-center text-sm font-semibold hover:bg-muted">Continue without signing in</Link><p className="mt-5 text-center text-[11px] leading-5 text-muted-foreground">Owner, Admin, and Team roles are represented in the demo data. Production authentication is documented in the roadmap.</p></Card></section></main>}
